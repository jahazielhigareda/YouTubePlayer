using System;

namespace YoutubePlayer.Core.Constants
{
    public enum MediaMusicOption
    {
        StopAndReturn = ConsoleKey.Escape,
        Stop = ConsoleKey.E,
        Pause = ConsoleKey.P,
        Play = ConsoleKey.Enter,
        IncrementVolume = ConsoleKey.S,
        DescrementVolume = ConsoleKey.D,
        MuteVolume = ConsoleKey.M
    }
}
